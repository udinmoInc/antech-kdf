---
title: "Node.js and TypeScript"
sidebarTitle: "Node / TS"
description: "Use the antech-kdf npm package with TypeScript types over the native FFI."
order: 34
---

```bash
./sdk/scripts/build-native.sh
cd bindings/node && npm install && npm run build
```

```typescript
import {
  hash,
  verify,
  hashWithConfig,
  defaultConfig,
  needsRehash,
  needsRehashWithPolicy,
  defaultRehashPolicy,
} from "antech-kdf";

const stored = hash("password");
verify("password", stored);

const cfg = defaultConfig();
cfg.memory_kib = 1024;
const custom = hashWithConfig("pw", cfg);

const policy = defaultRehashPolicy();
policy.preferred_memory_kib = 32768;
needsRehashWithPolicy(custom, policy);
```

Naming is camelCase in TypeScript; semantics match Rust and Python. Keep hashing off the event loop — see the async guide.

<Warning>
Do not call `hash` / `verify` on the main Node event-loop tick under load. Offload to a worker thread.
</Warning>

<NextSteps links={[
  { href: './overview', title: 'SDK overview' },
  { href: './conformance', title: 'Conformance' },
  { href: '../guides/async', title: 'Async guidance' }
]} />
