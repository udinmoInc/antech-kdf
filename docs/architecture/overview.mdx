---
title: "Architecture overview"
sidebarTitle: "Overview"
description: "How production crates, FFI, SDKs, and research stay separated around one engine."
order: 10
---

Antech keeps a hard line between **production** code and **research**. SDKs and attackers both depend on core; core never depends on research. That dependency direction is intentional: experimental cryptanalysis and GPU runners cannot leak into the shipping library.

## Dependency direction

| Crate / tree | Depends on |
|---|---|
| `antech-kdf` | `antech-kdf-core` |
| `antech-kdf-core` | `antech-kdf-format`, `antech-kdf-types` |
| `antech-kdf-ffi` | `antech-kdf` |
| `antech-kdf-cli` | `antech-kdf` |
| Language SDKs | `antech-kdf-ffi` |
| `research/code` | `antech-kdf-core` only |

Rule: research may import core; production never imports research.

## Production crates

| Crate | Responsibility |
|---|---|
| `antech-kdf` | Public `hash` / `verify` / rehash API |
| `antech-kdf-core` | `AntechEngine`, resource scheduler |
| `antech-kdf-format` | `$antech$v2$` encode and parse |
| `antech-kdf-types` | Config, errors, graph kinds |
| `antech-kdf-ffi` | C ABI used by every non-Rust SDK |
| `antech-kdf-cli` | Command-line hash / verify |

Research lives under `research/code/` as a **separate Cargo workspace**. Production workspace members never list research crates.

## Layer map

<FeatureGrid>
  <FeatureCard icon="cpu-chip" title="Engine" description="Seed bind, DAG walk, finalize." href="./engine" />
  <FeatureCard icon="share" title="Graph" description="CombinedFrontier parents and tiles." href="./graph" />
  <FeatureCard icon="document-text" title="Format" description="Self-describing v2 strings." href="./format" />
  <FeatureCard icon="link" title="FFI" description="Thread-safe C ABI for SDKs." href="./ffi" />
</FeatureGrid>

## What stays out of production

Attacker models, GPU kernels, TMTO explorers, and security-review runners live under `research/`. They may depend on `antech-kdf-core` for correct digests, but they are never linked into `antech-kdf` or language SDKs.

<NextSteps links={[
  { href: './engine', title: 'Canonical engine pipeline' },
  { href: '../security-review/overview', title: 'Security & review' }
]} />
