---
title: "Thread safety"
sidebarTitle: "Thread safety"
description: "Concurrent hash and verify calls share a host scheduler without caller locks."
order: 24
---

Public hash/verify entry points and FFI functions are **thread-safe**. Callers do not need a process-wide mutex around Antech.

Concurrent derives share `BoundedResourceScheduler`. Under pressure, requests fail with a resource error rather than over-allocating past the host ceiling.

Language SDKs that load the same `cdylib` inherit these properties. Keep wrappers free of global mutable crypto state.

<Related links={[
  { href: '../architecture/scheduler', title: 'Resource scheduler' },
  { href: './async', title: 'Async / non-blocking guidance' }
]} />
