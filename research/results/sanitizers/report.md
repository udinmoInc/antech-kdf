# ASan + UBSan validation report — Antech KDF

**Combined verdict: NOT_RUN**

Awaiting Ubuntu CI execution via `scripts/sanitizers_ci.sh` (ASan + UBSan jobs).

| Sanitizer | Verdict |
|---|---|
| AddressSanitizer | NOT_RUN |
| UndefinedBehaviorSanitizer | NOT_RUN |

Windows local host: **BLOCKED** for `-Zsanitizer=*`.

See `summary.md`, `asan.csv`, `ubsan.csv`, `skipped.csv`, `regressions.csv`, and `logs/` after CI completes.
