# Variant K1 (historical)

Research fork that fed password bytes into ARX rotation / feedback to stress SIMD and warp-friendly batching.

```text
S_{i+1} = ARX(S_i, Block[Addr_1] ^ Block[Addr_2], pwd_byte)
```

Measured on the early campaign host at 16 MiB: defender p50 ~108 ms, 16-core attacker ~19.2 g/s, TMTO ~4× at half memory. Not the production engine.
