# Reference implementation

Moved to [`../../code/reference/`](../../code/reference/) so all research Rust lives under `research/code/`.

```bash
cargo test --manifest-path research/code/Cargo.toml -p antech-kdf-reference --release
```
